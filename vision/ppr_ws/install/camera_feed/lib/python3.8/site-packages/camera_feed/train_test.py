from ultralytics import YOLO

# Start with a pre-trained YOLOv8 model
model = YOLO('yolov8n.pt')  # you can also use 's', 'm', or 'l' for larger models

# Train it on your data (using the data.yaml file in your current directory)
model.train(data='data.yaml', epochs=150)