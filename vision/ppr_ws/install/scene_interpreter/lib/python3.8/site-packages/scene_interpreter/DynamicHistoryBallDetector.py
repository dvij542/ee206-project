from .HistoryBallDetector import HistoryBallDetector
from .ball_detection_utils import detect_ball_dynamic

class DynamicHistoryBallDetector(HistoryBallDetector):
    def __init__(self, camera = -1, max_dist = 120):
        super().__init__(camera, max_dist, detect_ball_dynamic)